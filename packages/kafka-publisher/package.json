{
    "name": "@escrowly/kafka-publisher",
    "version": "1.0.0",
    "description": "Reliable Kafka event publishing using the Transactional Outbox Pattern",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "build:watch": "tsc --watch",
        "clean": "rimraf dist"
    },
    "peerDependencies": {
        "@escrowly/kafka-core": "^1.0.0",
        "@nestjs/common": "^10.0.0",
        "@nestjs/core": "^10.0.0"
    },
    "devDependencies": {
        "rimraf": "^5.0.0",
        "typescript": "^5.1.3"
    },
    "files": [
        "dist"
    ]
}
